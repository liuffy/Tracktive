import React from 'react';
import Item from './item';


Creator: <Link className="link" to={`/users/${tracktlist.user_id}`}> {tracktlist.user_id}</Link></span>

const state = getState();
					
					const user_id = state.session.currentUser.username;

					Songs: 